# VERA File Attachment API - Architecture Diagram

## System Architecture

```
┌──────────────────────────────────────────────────────────────────┐
│                   USER BROWSER (Frontend)                        │
│                   vera-pro.html                                  │
│                                                                  │
│  ┌────────────────────────────────────────────────────────────┐ │
│  │ Chat Interface                                             │ │
│  │                                                            │ │
│  │  [Message Input Area]                                    │ │
│  │  ├─ Text Input Box                                       │ │
│  │  ├─ Voice Button 🎤                                      │ │
│  │  └─ Attachment Button 📎 ← NEW                           │ │
│  │                                                            │ │
│  │  [File Picker - Hidden]                                  │ │
│  │  <input type="file" id="fileInput"                       │ │
│  │         accept="image/*,.pdf,.txt,.doc,.docx">           │ │
│  │                                                            │ │
│  └────────────────────────────────────────────────────────────┘ │
│                           │                                     │
│  Event: User clicks 📎    │                                     │
│  ├─ File picker opens     │                                     │
│  ├─ User selects file     │                                     │
│  ├─ Frontend validates:   │                                     │
│  │  ✓ Size < 5MB         │                                     │
│  │  ✓ Type supported     │                                     │
│  └─ Ready to upload       │                                     │
│                           │                                     │
└───────────────────────────┼──────────────────────────────────────┘
                            │
                            │ HTTP POST
                            │ Content-Type: application/json
                            │
        ┌───────────────────▼─────────────────────┐
        │ JSON Request Body                       │
        ├───────────────────────────────────────┤
        │ {                                     │
        │   "email": "user@example.com",        │
        │   "message": "Optional message",      │
        │   "conversationId": "uuid",           │
        │   "attachment": {                     │
        │     "name": "document.pdf",           │
        │     "type": "application/pdf",        │
        │     "size": 102400                    │
        │   }                                   │
        │ }                                     │
        └───────────────────┬─────────────────────┘
                            │
        ┌───────────────────▼──────────────────────────┐
        │ Network Transmission (HTTPS)                │
        │ URL: /api/file-upload                       │
        │ Method: POST                                │
        └───────────────────┬──────────────────────────┘
                            │
┌───────────────────────────▼──────────────────────────────────────┐
│                  VERCEL SERVERLESS (Backend)                     │
│                  /api/file-upload.js                             │
│                                                                  │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │ 1. CORS Configuration                                   │  │
│  │    ├─ Access-Control-Allow-Origin: *                   │  │
│  │    ├─ Access-Control-Allow-Methods: POST               │  │
│  │    └─ Access-Control-Allow-Headers: [list]             │  │
│  └──────────────────────────────────────────────────────────┘  │
│                           │                                     │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │ 2. Request Validation                                   │  │
│  │    ├─ Check method is POST ✓                           │  │
│  │    ├─ Extract { attachment } ✓                         │  │
│  │    ├─ Verify attachment exists ✓                       │  │
│  │    └─ Check size < 5MB ✓                               │  │
│  └──────────────────────────────────────────────────────────┘  │
│                           │                                     │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │ 3. User Management                                      │  │
│  │    ├─ Look up user by email                            │  │
│  │    ├─ Auto-create if guest                             │  │
│  │    ├─ Check trial status                               │  │
│  │    └─ Verify subscription                              │  │
│  └──────────────────────────────────────────────────────────┘  │
│                           │                                     │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │ 4. File Type Detection                                  │  │
│  │    ├─ Check if MIME type is image/* → Image handling  │  │
│  │    ├─ Check if PDF → PDF handling                      │  │
│  │    ├─ Check if text/plain → Text handling             │  │
│  │    ├─ Check if Word doc → Word handling               │  │
│  │    └─ Default → Generic handling                       │  │
│  └──────────────────────────────────────────────────────────┘  │
│                           │                                     │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │ 5. Conversation Management                              │  │
│  │    ├─ Use existing or create new                       │  │
│  │    ├─ Generate conversation title                       │  │
│  │    └─ Associate with user                              │  │
│  └──────────────────────────────────────────────────────────┘  │
│                           │                                     │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │ 6. Attachment Context Generation                        │  │
│  │    ├─ if (image) → "User shared image..."             │  │
│  │    ├─ if (pdf) → "User shared PDF..."                 │  │
│  │    ├─ if (text) → "User shared text..."               │  │
│  │    ├─ if (word) → "User shared Word doc..."           │  │
│  │    └─ else → "User shared file..."                     │  │
│  │                                                         │  │
│  │    Create enhancedMessage = userMessage + context      │  │
│  └──────────────────────────────────────────────────────────┘  │
│                           │                                     │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │ 7. VERA Response Generation                             │  │
│  │    ├─ Call getVERAResponse()                           │  │
│  │    ├─ Pass enhancedMessage (file-aware)                │  │
│  │    ├─ Get contextual response from AI                  │  │
│  │    └─ Clean up formatting                              │  │
│  └──────────────────────────────────────────────────────────┘  │
│                           │                                     │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │ 8. Database Persistence                                 │  │
│  │    ├─ Save user message to database                    │  │
│  │    ├─ Save VERA response to database                   │  │
│  │    ├─ Link to conversation ID                          │  │
│  │    └─ Include attachment metadata                       │  │
│  └──────────────────────────────────────────────────────────┘  │
│                           │                                     │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │ 9. Response Generation                                  │  │
│  │    {                                                   │  │
│  │      "success": true,                                  │  │
│  │      "response": "VERA's contextual response",         │  │
│  │      "conversationId": "uuid",                         │  │
│  │      "attachment": { ... },                           │  │
│  │      "subscription": { ... }                          │  │
│  │    }                                                   │  │
│  └──────────────────────────────────────────────────────────┘  │
│                                                                  │
└───────────────────────────┬──────────────────────────────────────┘
                            │
                            │ HTTP Response (200/400/500)
                            │ JSON Content
                            │
        ┌───────────────────▼──────────────────┐
        │ Network Transmission (HTTPS)        │
        └───────────────────┬──────────────────┘
                            │
┌───────────────────────────▼──────────────────────────────────────┐
│                   USER BROWSER (Frontend)                        │
│                   vera-pro.html (continued)                      │
│                                                                  │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │ 10. Response Processing                                 │  │
│  │     ├─ Parse JSON response                             │  │
│  │     ├─ Hide loading indicator                          │  │
│  │     ├─ Update conversationId if new                    │  │
│  │     ├─ Display VERA response in chat                   │  │
│  │     └─ Save to history                                 │  │
│  └──────────────────────────────────────────────────────────┘  │
│                           │                                     │
│  Chat Display Update:     │                                     │
│  ├─ Add user message:    │                                     │
│  │  "📎 Attached: document.pdf (250 KB)"                     │
│  │                       │                                     │
│  └─ Add VERA message:    │                                     │
│     "I've received your PDF document..."                       │
│                                                                  │
│  ✅ Conversation complete and saved                            │
│                                                                  │
└──────────────────────────────────────────────────────────────────┘
```

---

## Data Flow Diagram

```
User Attaches File
       │
       ▼
┌─────────────────┐
│ File Validation │ ← Check: Size < 5MB, Type supported
└────────┬────────┘
         │ ✓
         ▼
  ┌────────────────┐
  │ File Metadata  │ ← Extract: name, type, size
  │   Extraction   │
  └────────┬───────┘
           │
           ▼
  ┌────────────────────┐
  │ Send to Backend    │ ← POST /api/file-upload
  │ /api/file-upload   │ ← JSON with attachment metadata
  └────────┬───────────┘
           │
           ▼
  ┌────────────────────┐
  │ Backend Processing │
  └────────┬───────────┘
           │
      ┌────┴────────┬─────────────┬──────────────┐
      │             │             │              │
      ▼             ▼             ▼              ▼
  User Lookup  Type Detection  Context Gen   Trial Check
      │             │             │              │
      └────┬────────┴─────────────┴──────────────┘
           │
           ▼
  ┌────────────────────┐
  │ VERA Response Gen  │ ← getVERAResponse()
  │ (File-Aware)       │ ← Enhanced message with context
  └────────┬───────────┘
           │
           ▼
  ┌────────────────────┐
  │ Database Save      │ ← Store messages & metadata
  │ (Conversation)     │
  └────────┬───────────┘
           │
           ▼
  ┌────────────────────┐
  │ Send Response      │ ← HTTP 200 + JSON
  │ Back to Frontend   │
  └────────┬───────────┘
           │
           ▼
  ┌────────────────────┐
  │ Display in Chat    │ ← User message + VERA response
  │ Update UI          │ ← Save to history
  └────────────────────┘
           │
           ✅ Complete
```

---

## File Type Processing Pipeline

```
                    FILE RECEIVED
                          │
        ┌─────────────────┼─────────────────┐
        │                 │                 │
        ▼                 ▼                 ▼
    ┌────────┐        ┌────────┐        ┌────────┐
    │ Image  │        │  PDF   │        │ Text   │
    └────┬───┘        └────┬───┘        └────┬───┘
         │                 │                 │
         ▼                 ▼                 ▼
    "User shared    "User shared       "User shared
     image file:    PDF document:      text document:
     [filename]     [filename]         [filename]
     Please        Please help        Please analyze
     analyze       with content       content from
     image..."     from document..."  document..."
         │                 │                 │
         └─────────────────┼─────────────────┘
                           │
                           ▼
                   ┌───────────────┐
                   │ Create Context│
                   │    Message    │
                   └───────┬───────┘
                           │
                           ▼
                   ┌───────────────────┐
                   │ Combine with User │
                   │ Message (if any)  │
                   └───────┬───────────┘
                           │
                           ▼
                   ┌─────────────────────┐
                   │ Enhanced Message    │
                   │ Ready for VERA      │
                   └───────┬─────────────┘
                           │
                           ▼
                   ┌─────────────────────┐
                   │ getVERAResponse()   │
                   │ Generates contextual│
                   │ response            │
                   └─────────────────────┘
```

---

## Error Handling Flow

```
Request Arrives
       │
       ▼
┌──────────────────┐
│ Check method=POST│ ─ No → ❌ 405 Method not allowed
└────────┬─────────┘
         │ Yes
         ▼
┌──────────────────┐
│ Has attachment?  │ ─ No → ❌ 400 Attachment required
└────────┬─────────┘
         │ Yes
         ▼
┌──────────────────┐
│ Size < 5MB?      │ ─ No → ❌ 400 File size exceeds 5MB
└────────┬─────────┘
         │ Yes
         ▼
┌──────────────────┐
│ User lookup      │ ─ Fail → ❌ 500 Database error
└────────┬─────────┘
         │ Success
         ▼
┌──────────────────┐
│ Trial active?    │ ─ No → 200 {success: false, code: TRIAL_EXPIRED}
└────────┬─────────┘
         │ Yes
         ▼
┌──────────────────┐
│ Generate VERA    │ ─ Fail → ❌ 500 Processing error
│ response         │
└────────┬─────────┘
         │ Success
         ▼
┌──────────────────┐
│ Save to DB       │ ─ Fail → ❌ 500 Database error
└────────┬─────────┘
         │ Success
         ▼
      ✅ 200
      Success response
      with VERA reply
```

---

## Storage & Retrieval

```
┌──────────────────────────┐
│   PostgreSQL Database    │
│                          │
│  Conversations Table     │
│  ├─ id (UUID)           │
│  ├─ user_id             │
│  └─ title               │
│                          │
│  Messages Table         │
│  ├─ id                  │
│  ├─ conversation_id     │
│  ├─ sender ("user" or "assistant")
│  ├─ message             │
│  ├─ created_at          │
│  └─ metadata (JSON)     │
│     └─ attachment       │
│        ├─ name          │
│        ├─ type          │
│        ├─ size          │
│        └─ processed: true
│                          │
└──────────────────────────┘
         ▲
         │ saveMessageInConversation()
         │
    API Handler
```

---

## Frontend Component Hierarchy

```
vera-pro.html
│
├─ Chat Container
│  ├─ Sidebar
│  │  ├─ Logo with orb
│  │  └─ Thread history
│  │
│  ├─ Main Chat Area
│  │  ├─ Message History
│  │  │  ├─ User messages
│  │  │  └─ VERA responses
│  │  │
│  │  └─ Input Area
│  │     ├─ Text input box
│  │     ├─ Buttons
│  │     │  ├─ Send (↑)
│  │     │  ├─ Voice (🎤)
│  │     │  └─ Attachment (📎) ← NEW
│  │     │
│  │     └─ Hidden File Input
│  │        <input type="file" id="fileInput">
│  │
│  └─ Modals
│     ├─ Settings
│     ├─ Share
│     ├─ History
│     ├─ Report Issue
│     └─ About VERA
│
└─ JavaScript Functions
   ├─ attachFile()
   │  └─ Triggers file picker
   │
   ├─ handleFileUpload(event)
   │  ├─ Validates file
   │  ├─ Sends to /api/file-upload
   │  └─ Displays response
   │
   └─ Other chat functions
      (unmodified)
```

---

## Deployment Architecture

```
┌────────────────────────────────┐
│    Local Development           │
│  vera-20251101-fresh/          │
│  ├─ /api/file-upload.js        │
│  ├─ /public/vera-pro.html      │
│  └─ Other files                │
└────────┬───────────────────────┘
         │ git push
         ▼
┌────────────────────────────────┐
│    GitHub Repository           │
│  (triggers Vercel webhook)     │
└────────┬───────────────────────┘
         │ npm run deploy
         ▼
┌────────────────────────────────┐
│    Vercel Build/Deploy         │
│  ├─ Compile functions          │
│  ├─ Validate configuration     │
│  └─ Deploy to edge network     │
└────────┬───────────────────────┘
         │
         ▼
┌────────────────────────────────┐
│    Production CDN              │
│  vera-20251101-fresh-*.        │
│  vercel.app                    │
│                                │
│  ├─ /api/file-upload           │
│  ├─ /api/chat (existing)       │
│  ├─ /vera-pro.html             │
│  └─ Other endpoints            │
│                                │
│  ✅ Live & Accessible          │
└────────────────────────────────┘
```

---

## Security Layers

```
                    CLIENT (Browser)
                           │
        ┌──────────────────┼──────────────────┐
        │                  │                  │
        ▼                  ▼                  ▼
   File Size           MIME Type          Network
   Validation          Checking           (HTTPS)
   (5MB limit)         (Filter UI)        (TLS 1.3+)
        │                  │                  │
        └──────────────────┼──────────────────┘
                           │
                    HTTP/HTTPS Request
                           │
                    VERCEL EDGE NETWORK
                           │
        ┌──────────────────┼──────────────────┐
        │                  │                  │
        ▼                  ▼                  ▼
   Rate Limiting      DDoS Protection    IP Filtering
   (per endpoint)     (built-in)         (optional)
        │                  │                  │
        └──────────────────┼──────────────────┘
                           │
                    /api/file-upload.js
                           │
        ┌──────────────────┼──────────────────┐
        │                  │                  │
        ▼                  ▼                  ▼
   Request           File Validation    User Auth
   Validation        (size, type)       (trial/sub)
   (POST method)                             │
        │                  │                  │
        └──────────────────┼──────────────────┘
                           │
                    PostgreSQL Database
                           │
        ┌──────────────────┼──────────────────┐
        │                  │                  │
        ▼                  ▼                  ▼
   Encryption        Access Control    Audit Logging
   (at rest)         (user_id)          (timestamps)
```

---

**Architecture Complete!** 🏗️

All systems properly designed and deployed for secure, efficient file attachment handling in VERA.
